import React from "react";

// style
import "../../assets/scss/main.scss";

type Props = {};

const Header = (props: Props) => {
  return (
    <header>
      <h1>SPLITTER</h1>
    </header>
  );
};

export default Header;
